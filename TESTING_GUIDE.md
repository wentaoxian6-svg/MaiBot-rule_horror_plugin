# 规则怪谈插件 - 测试指南

适用版本：`2.3.0`

当前插件目录没有自带 `tests/` 自动化测试目录，本指南以手动验收和运行日志核对为主。

## 测试前准备

### 1. 配置可用的 `config.toml`

至少确认以下字段已经填好：

```toml
[plugin]
enabled = true
config_version = "2.3.0"

[llm]
api_url = "https://api.deepseek.com/chat/completions"
api_key = ""
model_list = []
temperature = 1.0
max_tokens = 8000

[[llm.models]]
name = "deepseek-v4-pro"
enabled = true
api_url = "https://api.deepseek.com/chat/completions"
api_key = ""
extra_body = { thinking = { type = "enabled" }, reasoning_effort = "high" }

[npc_sim]
enabled = true
trigger_on_every_action = true
room_hearing_radius = 1
max_event_history = 20
api_url = ""
api_key = ""
model_list = []
```

注意：

- 当前版本正式加载 `[plugin]`、`[llm]`、`[[llm.models]]`、`[npc_sim]`、`[[npc_sim.models]]`、`[save]`

### 2. 安装依赖

```bash
uv pip install -r requirements.txt
```

如果你在 Linux 上验收，建议额外确认：

- 已安装中文字体，例如 `Noto Sans CJK` 或 `文泉驿微米黑`
- 如果字体不在默认路径，已设置 `RULE_HORROR_FONT` 或 `plugin.font_path`
- 若插件目录是只读挂载，`XDG_DATA_HOME` 或 `HOME` 至少有一个可写

### 3. 启动并确认插件已加载

启动 MaiBot 后，确认插件没有在加载阶段报错，再开始游戏内测试。

## 验收重点

本版本建议至少覆盖以下几类风险：

- 版本与配置口径是否统一为 `2.3.0`
- 新版 SDK 入口与配置结构是否可正常加载
- 自然语言引导是否不再自动提取规则
- 规则载体与 `/rg 规则` 是否正确落到玩家规则笔记
- 多人身份任务卡是否不再直接泄露规则正文
- 不同身份/组别的规则载体可见性是否正确
- 房间级 NPC 位置、同房间可见、相邻房间可听是否正确
- LLM 调用、图片生成、存档是否可用
- 基础游玩链路是否能完整跑通

## 手动测试流程

### 阶段 1：基础开局

#### 1.1 开始单人游戏

```text
/rg 开始 单人
```

预期结果：

- 收到“正在生成规则怪谈，请稍候..”或类似提示
- 收到剧情导入图
- 开场最后会收到一张标题为“目标”的目标图
- 如果开场是 `rule_carrier` 形式，则继续收到相应规则图或私聊载体信息
- 如果开场是 `natural_language` 形式，则不会自动收到规则发送，这属于正常结果
- 收到“游戏已开始”类提示

检查点：

- [ ] 插件没有抛出明显异常
- [ ] 图片能正常发送
- [ ] 场景、规则、通关条件都已生成

#### 1.2 查看基础信息

依次执行：

```text
/rg 状态
/rg 剧情
/rg 规则
/rg 场景
/rg 区域
```

预期结果：

- `/rg 状态` 能看到玩家状态
- `/rg 剧情` 能重新发送导入内容
- `/rg 规则` 展示当前规则笔记
- `/rg 场景` 展示场景整体印象（如四周氛围、通道）及玩家当前所在地，不显示区域清单或探索建议
- `/rg 区域` 以编号列表展示场景中全部可确认区域，包含普通区域和特殊区域且不重复

#### 1.3 目标与临近通关

在接近通关前，确认目标图只显示一句简短、可判定的通关条件。准备条件齐全且下一次行动即可完成目标时，预期会收到“尽快完成目标”或“继续探索怪谈根源”的选择提示；仍需要搜集、推理或完成多步操作时，不应触发该提示。

#### 1.4 规则笔记

依次执行：

```text
/rg 规则
/rg 记录规则 不要在铃响后回头
/rg 规则
```

预期结果：

- 初次查看规则时，如果尚未整理，显示“规则笔记为空”一类提示
- 记录后能看到刚写入的规则
- 重复记录同一条规则时不会重复追加

#### 1.5 NPC 房间级感知

在单人或多人中连续执行：

```text
/rg 行动 去值班室
/rg 行动 检查门外的动静
/rg 行动 靠近门口听里面有什么声音
```

预期结果：

- 如果 NPC 与玩家同房间，结果文本中会出现“此刻也在这里”之类的直接可见描述
- 如果 NPC 在相邻房间，结果文本中会出现“隐约听见附近有动静”之类的可听描述
- 距离更远时，不应稳定出现这些动静提示

### 阶段 2：行动、道具与提示

#### 2.1 普通行动

```text
/rg 行动 我检查房间角落
/rg 行动 我打开柜子
```

预期结果：

- 能生成行动结果
- 状态变化、描述内容和发现结果基本合理

#### 2.2 道具和休息

```text
/rg 行动 喝水
/rg 行动 吃面包
/rg 行动 休息
/rg 行动 休息30分钟
```

预期结果：

- 命中道具/休息时可直接返回效果
- 体力、疲劳、压力、焦虑等数值出现对应变化

#### 2.3 提示

```text
/rg 提示 规则
/rg 提示 线索
```

预期结果：

- 能收到提示内容
- 提示次数会递减
- 用尽后会提示无法继续获取

### 阶段 3：理智与规则变化

#### 3.1 理智变化

执行数次高风险行动，例如：

```text
/rg 行动 我打开那扇不该打开的门
/rg 行动 我触碰墙上的奇怪痕迹
/rg 行动 我继续深入调查
```

预期结果：

- 理智值会下降
- 描述风格会随着理智降低变得更混乱或更不安

#### 3.2 理智崩坏

当理智接近 `0` 时继续推进，重点观察：

- 行动结果图是否进入崩坏展示
- 状态栏是否按当前实现调整显示
- 不会因为这次判定死亡就直接退化成极简错误输出

#### 3.3 规则变异

持续搜索关键物品，例如：

```text
/rg 行动 我检查抽屉
/rg 行动 我查看墙上的字迹
/rg 行动 我翻找桌面
```

预期结果：

- 发现关键物品后，规则可能出现变化
- 理智崩坏时不应期待规则变异稳定触发

### 阶段 4：存档链路

#### 4.1 保存与读取

```text
/rg 保存 测试节点
/rg 存档列表
/rg 读取 测试节点
/rg 恢复
```

预期结果：

- 命名存档成功
- 能在存档列表中看到记录
- 读取后状态与保存点大致一致
- 默认存档恢复可用

### 阶段 5：多人身份与差异化规则暴露

#### 5.1 多人开局

```text
/rg 开始 多人
/rg 加入
/rg 开始 多人 开始
```

预期结果：

- 每位玩家收到的是身份任务卡，不是完整规则清单
- `/rg 身份` 重发的仍然是私聊身份任务卡
- 私聊失败时，群内只提示补救，不泄露身份正文

#### 5.2 多人差异化探索

让不同玩家分别去不同区域执行：

```text
/rg 行动 检查本区域的告示
/rg 行动 搜索桌面
/rg 行动 观察附近 NPC 的行为
```

预期结果：

- 不同身份、不同区域的玩家可能发现不同规则载体
- 某位玩家发现并记录的规则，不会自动同步到其他玩家的 `/rg 规则`
- 某些规则可能只能通过观察 NPC 行为或对比多人信息推理出来

#### 5.3 清理

```text
/rg 清理存档
```

预期结果：

- 返回已结束存档数量和过期图片缓存数量
- 无明显异常

### 阶段 6：结局

```text
/rg 继续
/rg 结束
```

预期结果：

- 达成通关条件后可继续探索
- `/rg 结束` 能生成结局图
- 完美、成功、通关、失败等结果按当前局面输出



## 常见问题排查

### 图片不显示

- 查看日志确认是否生成图片失败
- 检查数据目录和 `temp_images` 是否可写
- 检查字体路径是否可用
- Linux 下还应检查最终回退的数据目录是否落到了 `${XDG_DATA_HOME}`、`~/.local/share` 或系统临时目录

### LLM 调用失败

- 检查 `api_url`、`api_key`、模型名
- 查看网关是否真的兼容 Chat Completions 返回格式
- 查看日志中的状态码和响应正文

### 多人身份没有私聊到达

- 先确认机器人能否与目标玩家建立私聊
- 当前实现不会回退到群聊发送身份正文；修复私聊权限后重新使用 `/rg 身份`

### 存档恢复异常

- 先查看 `/rg 存档列表`
- 再检查默认存档和命名存档文件是否存在

### Linux 专项检查

- [ ] 插件目录只读时仍能正常生成图片和存档
- [ ] 未配置字体时能在日志中看到明确字体探测告警
- [ ] 配置 `RULE_HORROR_FONT` 后长图中文显示正常
- [ ] `/rg 清理存档` 不会误删最近刚生成的图片

## 测试记录模板

```text
测试日期：____年__月__日
测试人员：________
MaiBot 版本：________
插件版本：2.3.0

【基础开局】
- [ ] 单人开局
- [ ] 状态查看
- [ ] 剧情查看
- [ ] 规则查看
- [ ] 场景查看

【行动链路】
- [ ] 普通行动
- [ ] 道具使用
- [ ] 休息处理
- [ ] 提示次数

【理智与规则】
- [ ] 理智下降
- [ ] 理智崩坏展示
- [ ] 关键物品触发规则变化

【存档】
- [ ] 手动保存
- [ ] 读取命名存档
- [ ] 恢复默认存档
- [ ] 清理存档

【多人】
- [ ] 创建大厅
- [ ] 玩家加入
- [ ] 身份私聊发送
- [ ] 私聊失败仅提示补救
- [ ] 多人独立行动

【架构与命令层】
- [ ] `plugin.py` 仅保留入口与装配
- [ ] `flows/` 单人和多人流程可正常导入
- [ ] `commands/shared_handlers.py` 共享命令链路可正常使用

【结局】
- [ ] 继续探索
- [ ] 结束游戏
- [ ] 结局图片

【文档一致性】
- [ ] 版本统一为 2.3.0
- [ ] 无独立 environment 配置误导
- [ ] LLM 能力描述真实
- [ ] 多人身份说明真实

问题记录：
1. ________
2. ________
3. ________
```

## 通过标准

满足以下条件即可认为本轮验证通过：

- 基础单人链路可完整跑通
- 多人大厅与身份发送行为符合当前实现
- 存档、读取、结局至少各验证一次
- 文档口径和代码事实没有明显冲突
