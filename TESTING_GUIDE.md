# 规则怪谈插件 - 测试指南

## 测试前准备

### 1. 确认配置
检查 `config.toml` 文件：
```toml
[plugin]
enabled = true

[llm]
api_url = "http://rinkoai.com/v1/chat/completions"
api_key = "YOUR_API_KEY"  # 确保填入正确的API密钥
model_list = ["deepseek-ai/DeepSeek-V3"]
temperature = 0.8
```

**备注**：
- 使用 `/rg 行动 <描述>` 命令格式进行游戏行动。
- 使用 `/rg 结束` 结束游戏并判定结局。


### 2. 确认依赖
```bash
pip install -r requirements.txt
```

### 3. 重启MaiBot
确保插件已加载

## 测试流程

### 阶段1：基础功能测试（必测）

#### 1.1 开始游戏
```
/rg 开始 单人
```
**预期结果**：
- 收到"正在生成规则怪谈，请稍候..."消息
- 收到剧情导入长图（黑暗背景+鲜红字体）
- 收到规则长图
- 收到游戏开始提示

**检查点**：
- [ ] 图片是否正常显示
- [ ] 场景名称是否合理
- [ ] 规则是否有5-8条
- [ ] 通关条件是否明确

#### 1.2 查看状态
```
/rg 状态
```
**预期结果**：
- 显示游戏状态
- 显示玩家信息（理智100/100，体力100/100）
- 显示提示次数（0/3）

#### 1.3 查看规则
```
/rg 规则
```
**预期结果**：
- 显示所有规则
- 显示通关条件

#### 1.4 查看场景
```
/rg 场景
```
**预期结果**：
- 显示场景结构
- 显示建筑类型、楼层布局等

### 阶段2：理智值系统测试（重点）

#### 2.1 正常理智值行动（理智100）
```
/rg 行动 我小心翼翼地检查房间
```
**预期结果**：
- 收到行动结果长图
- 场景描述客观清晰、冷静理性
- 理智值轻微下降（-1到-5）

**检查点**：
- [ ] 图片正常显示
- [ ] 描述风格正常（客观、理性）
- [ ] 理智值和体力值正确显示
- [ ] 状态栏完整

#### 2.2 降低理智值
连续执行危险行动，降低理智值到30以下：
```
/rg 行动 我打开了那扇被禁止打开的门
/rg 行动 我凝视着墙上的诡异图案
/rg 行动 我触摸了那个散发着不祥气息的物体
```

**预期结果**：
- 每次行动理智值下降
- 当理智值<30时，描述开始变得混乱
- 出现幻觉、错觉的描述

**检查点**：
- [ ] 理智值正确下降
- [ ] 描述风格随理智值变化
- [ ] 理智值<30时出现红色涂鸦效果

#### 2.3 理智崩坏（理智0）
继续执行危险行动，直到理智值为0：
```
/rg 行动 我继续深入探索
```

**预期结果**：
- 理智值降到0
- 行动结果长图进入“崩坏展示”（深红/血色风格、叙述更直接）
- 只展示场景叙述/行动反馈，不展示状态栏
- **即使这次行动判定死亡，也应当仍然生成“行动结果长图”（不会退化成只剩三行的死亡提示）**
- 不会出现强制的 OOC 指令提示（如“请使用 `/rg 结束`”）

**检查点**：
- [ ] 图片风格明显变化（深红/血色、压迫感）
- [ ] 叙述风格完全改变（诱导性、温柔但诡异/否认死亡）
- [ ] 不显示状态栏
- [ ] 使用 `/rg 结束` 结束游戏


### 阶段3：环境记忆测试

#### 3.1 访问多个位置
```
/rg 行动 我走向大厅
/rg 行动 我进入走廊
/rg 行动 我返回大厅
```

**预期结果**：
- 第一次访问大厅：详细描述
- 第二次访问大厅：简化描述或提及"你再次来到这里"

**检查点**：
- [ ] 环境记忆正常工作
- [ ] 避免重复描述

#### 3.2 互动物体
```
/rg 行动 我打开柜子
/rg 行动 我再次检查柜子
```

**预期结果**：
- 第一次互动：详细描述
- 第二次互动：简化描述

### 阶段4：规则变异测试

#### 4.1 触发规则变异（关键物品）
持续探索，直到**发现关键物品**（关键物品的判定由 LLM 决定，可能需要多次探索/检查才会遇到）：
```
/rg 行动 我检查柜子里有什么
/rg 行动 我查看墙上的字迹
/rg 行动 我翻找抽屉
/rg 行动 我调查房间角落
```

**预期结果**：
- 当行动结果中“发现了关键物品”时，系统会触发规则变异流程
- 规则可能发生变化（通常是对 1-2 条规则进行细微但令人不安的篡改/补充）
- 如果玩家理智值为 0（理智崩坏），**不会触发规则变异**

**检查点**：
- [ ] 找到关键物品后，规则是否出现变化迹象
- [ ] 变异后的规则是否“简洁、直接、令人不安”，且与剧情推进相关

#### 4.2 查看规则变化
```
/rg 规则
```

**预期结果**：
- 显示当前规则（如发生变异，应能在此看到变异后的规则）


### 阶段5：多人模式测试

#### 5.1 开始多人游戏
```
/rg 开始 多人
```

**预期结果**：
- 收到多人模式开始提示
- 提示其他玩家使用 `/rg 加入` 加入游戏
- 创建多人大厅
- 房主在人数到齐后使用 `/rg 开始 多人 开始` 生成开局并开始游戏


#### 5.2 其他玩家加入
```
/rg 加入
```

**预期结果**：
- 玩家成功加入
- 显示当前玩家列表

#### 5.3 房主生成开局
```
/rg 开始 多人 开始
```

**预期结果**：
- 开始生成规则怪谈
- 生成人数匹配的玩家身份
- 开始游戏并进入可行动状态


#### 5.4 独立行动
每个玩家独立执行行动：
```
玩家A: /rg 行动 我检查大厅
玩家B: /rg 行动 我探索走廊
```

**预期结果**：
- 每个玩家收到独立的行动结果
- 每个玩家的理智值独立变化

#### 5.5 查看身份
```
/rg 身份
```

**预期结果**：
- 显示玩家当前身份
- 显示身份描述
- 显示该身份特有的规则
- 显示该身份独有的信息

**检查点**：
- [ ] 身份信息显示正确
- [ ] 独有规则清晰
- [ ] 独有信息有助于游戏

#### 5.6 协作规则（如果生成）
查看是否有协作规则，尝试触发：
```
玩家A: /rg 行动 我站在大厅中央
玩家B: /rg 行动 我站在走廊尽头
```

**预期结果**：
- 如果满足协作条件，触发特殊事件
- 获得协作奖励（线索或真相）

#### 5.7 物理存在感系统
测试玩家间的物理交互：
```
玩家A: /rg 行动 我走向玩家B
玩家B: /rg 行动 我观察玩家A的行动
```

**预期结果**：
- 系统记录玩家位置
- 同位置玩家可以互相看到
- 距离影响可见性和听觉

**检查点**：
- [ ] 位置系统正常工作
- [ ] 可见性判定正确
- [ ] 玩家可以互相感知

### 阶段6：NPC系统测试

#### 6.1 NPC互动
在游戏中遇到NPC时尝试互动：
```
/rg 行动 我和医生对话
/rg 行动 我问护士关于规则的事情
```

**预期结果**：
- NPC根据态度回应玩家
- NPC有记忆系统，能记住之前的互动
- NPC态度会根据玩家行为变化

**检查点**：
- [ ] NPC回应合理
- [ ] 态度系统正常工作
- [ ] NPC记忆功能正常

#### 6.2 NPC态度变化
尝试改变NPC态度：
```
/rg 行动 我对医生表示友好
/rg 行动 我威胁护士
```

**预期结果**：
- 友好行为提升好感度
- 威胁行为增加恐惧度或敌意度
- NPC态度影响对话内容

### 阶段7：环境演化系统测试

#### 7.1 环境变化
观察环境随时间的变化：
```
/rg 行动 我观察周围的环境
（等待一段时间）
/rg 行动 我再次观察周围的环境
```

**预期结果**：
- 环境状态随时间演化
- 光线、声音、气味可能发生变化
- 环境变化影响游戏氛围

**检查点**：
- [ ] 环境状态记录正确
- [ ] 环境变化自然合理
- [ ] 氛围营造有效

#### 7.2 随机事件
持续游戏，观察随机事件：
```
/rg 行动 我继续在走廊探索
```

**预期结果**：
- 可能触发随机事件
- 事件与场景类型和建筑类型相关
- 事件增加游戏不确定性

### 阶段8：提示系统测试

#### 8.1 获取规则提示
```
/rg 提示 规则
```

**预期结果**：
- 收到规则相关的提示
- 提示次数减少（2/3）

#### 8.2 获取线索提示
```
/rg 提示 线索
```

**预期结果**：
- 收到线索相关的提示
- 提示次数减少（1/3）

#### 8.3 用完提示次数
```
/rg 提示 规则
```

**预期结果**：
- 收到最后一次提示
- 提示次数为0

```
/rg 提示 规则
```

**预期结果**：
- 提示"提示次数已用完"

### 阶段9：存档系统测试

#### 9.1 手动保存
```
/rg 保存 测试存档1
```

**预期结果**：
- 提示保存成功

#### 9.2 查看存档列表
```
/rg 存档列表
```

**预期结果**：
- 显示所有存档
- 包括默认存档和手动存档

#### 9.3 读取存档
```
/rg 读取 测试存档1
```

**预期结果**：
- 游戏状态恢复到保存时的状态
- 理智值、体力值、位置等都正确恢复

### 阶段10：结局判定测试

#### 10.1 达成通关条件
根据通关条件执行相应行动

**预期结果**：
- 收到"恭喜！你已达成通关条件！"消息
- 提示可以继续探索或结束游戏

#### 10.2 继续探索
通关后使用继续探索功能：
```
/rg 继续
```

**预期结果**：
- 可以继续游戏，追求更完美的结局
- 系统会记录通关状态
- 可以继续探索隐藏内容

#### 10.3 结束游戏
```
/rg 结束
```

**预期结果**：
- 收到"正在判定结局..."消息
- 收到结局长图（黑暗背景+鲜红字体）
- 显示结局类型（完美/成功/通关/失败）
- 完美、成功、通关结局会显示推理分析
- 完美、成功结局在满足条件时会显示隐藏真相
- 失败结局不会显示推理分析和隐藏真相
- 玩家未通关就结束游戏时不会显示推理分析和隐藏真相


**检查点**：
- [ ] 结局判定合理
- [ ] 结局图片正常显示
- [ ] 推理分析详细
- [ ] 真相揭示（如果满足条件）

## 性能测试

### 1. 图片生成速度
- 观察图片生成是否流畅
- 是否有明显延迟

### 2. LLM响应时间
- 观察行动判定的响应时间
- 是否在可接受范围内（5-15秒）

### 3. 存档保存速度
- 观察存档保存是否快速
- 是否有卡顿

## 常见问题排查

### 问题1：图片不显示
**可能原因**：
- 图片生成失败
- 图片发送失败
- 权限问题

**排查方法**：
1. 查看日志文件
2. 检查 `data/temp_images/` 目录
3. 确认图片文件是否生成

### 问题2：LLM调用失败
**可能原因**：
- API密钥错误
- 网络连接问题
- 模型不可用

**排查方法**：
1. 检查 `config.toml` 中的API配置
2. 测试网络连接
3. 查看日志文件

### 问题3：理智值不变化
**可能原因**：
- LLM返回的JSON格式错误
- 状态更新失败

**排查方法**：
1. 查看日志文件
2. 检查LLM返回的数据
3. 确认状态更新逻辑

### 问题4：规则不变异
**可能原因**：
- 尚未发现关键物品（规则变异主要由“关键物品”触发）
- 玩家理智值为 0（理智崩坏时不触发规则变异）
- 规则变异评估被判定为“不需要变化”（LLM 返回 `should_mutate != "是"`）
- LLM调用失败

**排查方法**：
1. 继续探索/调查，尝试触发“发现关键物品”
2. 确认当前理智值不为 0
3. 查看日志文件，重点关注规则变异评估/生成的错误信息


## 测试报告模板

```
测试日期：____年__月__日
测试人员：________
MaiBot版本：________
插件版本：v2.1.0

【基础功能】
- [ ] 开始游戏：通过/失败
- [ ] 查看状态：通过/失败
- [ ] 查看规则：通过/失败
- [ ] 查看场景：通过/失败

【理智值系统】
- [ ] 正常理智值（>70）：通过/失败
- [ ] 中等理智值（40-70）：通过/失败
- [ ] 低理智值（<30）：通过/失败
- [ ] 理智崩坏（=0）：通过/失败
- [ ] 图片效果：通过/失败

【环境记忆】
- [ ] 位置记录：通过/失败
- [ ] 物体互动记录：通过/失败
- [ ] 避免重复描述：通过/失败

【规则变异】
- [ ] 触发条件：通过/失败
- [ ] 变异效果：通过/失败
- [ ] 冷却时间：通过/失败

【多人模式】
- [ ] 加入游戏：通过/失败
- [ ] 查看身份：通过/失败
- [ ] 独立状态：通过/失败
- [ ] 物理存在感：通过/失败
- [ ] 协作规则：通过/失败

【NPC系统】
- [ ] NPC互动：通过/失败
- [ ] 态度变化：通过/失败
- [ ] NPC记忆：通过/失败

【环境演化系统】
- [ ] 环境变化：通过/失败
- [ ] 随机事件：通过/失败

【提示系统】
- [ ] 规则提示：通过/失败
- [ ] 线索提示：通过/失败
- [ ] 次数限制：通过/失败

【存档系统】
- [ ] 手动保存：通过/失败
- [ ] 读取存档：通过/失败
- [ ] 存档列表：通过/失败

【结局判定】
- [ ] 通关判定：通过/失败
- [ ] 结局生成：通过/失败
- [ ] 真相揭示：通过/失败

【性能】
- 图片生成速度：____秒
- LLM响应时间：____秒
- 存档保存速度：____秒

【问题记录】
1. ________
2. ________
3. ________

【总体评价】
________
```

## 测试完成标准

所有测试项目通过，且：
1. 无严重bug
2. 性能在可接受范围内
3. 用户体验良好
4. 功能完整可用

---

**祝测试顺利！** 🎮
